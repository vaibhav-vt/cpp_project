#include"Animate.hpp"


/*void Animate :: update(std :: vector<std :: pair<int,int>>& images)
{
    // std :: cout << "Yup";
    srcRect.h = 32;
    srcRect.w = 32;
 
    destRect.x = 0;
    destRect.y = 0;
    destRect.w = srcRect.w*f;
    destRect.h = srcRect.h*fy;
    
    srcRect.x = 0;
    srcRect.y = 0;
    int t = (SDL_GetTicks()/100) % 5;
    
    srcRect.x = images[t].first;
    srcRect.y = images[t].second;
} */